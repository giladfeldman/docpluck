# Handoff — v2.4.79: canary findings #1 + #2 cleared; canary-gate substrate hardened (2026-06-07)

**Status: v2.4.79 COMMITTED to `main` (`76a0248`), NOT pushed, NOT tagged.**
Cycle 5 of the canary-iterate run. Cleared ip_feldman canary findings #1 (METADATA-LEAK) and #2 (HALLUCINATION false-positive). Hardened the canary-gate substrate (line-number-invariant finding keys, `allowed_omissions`, hooks wired to `--gate-new-only`). 10 deferred canary findings remain (Camelot tables + R4 reading-order — multi-session). Gates: full pytest **1870 passed**, **26/26** corpus baseline, substrate self-tests **17/17**.

---

## What landed in v2.4.79 (`76a0248`)

Library (NORMALIZATION_VERSION 1.9.27 → 1.9.28), both keyed on structural signatures:

| Fix | Location | Cleared |
|---|---|---|
| US-format publication-history line strip — `Received Month DD, YYYY; revision accepted Month DD, YYYY` (date sub-pattern accepts either order; anchored on `revision accepted` + year) | `normalize.py` `_PAGE_FOOTER_LINE_PATTERNS` | canary #1 (METADATA-LEAK) |
| Continuation-demoter **rejoin** — demoted false-heading is rejoined to the prior line it continues + terminal period restored, instead of left as an orphan bare line | `render.py` `_demote_continuation_promoted_headings` | canary #2 (HALLUCINATION false-positive) |

Finding #2 was confirmed an **audit false-positive** — the sentence (`The calculated effect sizes are summarized in the Supplemental Materials.`) is real (gold line 86). The actual defect was a spurious mid-sentence split + dropped period (a demote-miss the function's docstring had deferred in cycle 3).

Tests: `tests/test_normalize_metadata_leak_real_pdf.py` (US Received-line contract + variants + real-PDF), `tests/test_hallucinated_heading_continuation_guard.py` (`TestContinuationRejoin` + real-PDF).

### Substrate fixes (shared `~/.claude/skills/_shared/iterate-loop/`, not in this repo's git history)

1. **`finding_key` line-number-invariance** (`canary_findings.py` `_norm_location`): line numbers are no longer part of a finding's identity. A render change that shifts lines was re-opening KNOWN deferred findings as NEW and falsely blocking commits — it defeated `--gate-new-only` entirely. + invariance self-test.
2. **`allowed_omissions`** (new optional audit-payload field, wired through `canary-audit.sh` + `audit-subagent-prompt.md`, populated in `.claude/skills/_project/canary.json`): the Sonnet auditor no longer flags docpluck's by-design front-matter masthead / publication-history strips as TEXT-LOSS.
3. **Git hooks wired to `--gate-new-only`** (`.git/hooks/pre-commit`, `pre-push`): pre-commit + push-to-main gate on NEW/regressed findings only; **tag** push keeps the strict no-exceptions gate (a release tag must ship a fully-clean canary).
4. Fixed a backslash-in-string-literal bug in the new finding-key self-test.

The docpluck ledger (`.claude/skills/_project/canary-findings-ledger.json`, untracked) was migrated to robust keys: **#1/#2 RESOLVED**, 10 deferred findings OPEN.

---

## Remaining 10 deferred canary findings on ip_feldman (all multi-session — surfaced, NOT hacked)

All are the two big architecture tracks. **Pick ONE deliberately next session; do not bundle.**

- **Camelot tables (B4)** — Table 2 hypothesis content + "A positive number indicates…" footnote serialized mid-Introduction; Table 10 caption + numeric rows serialized mid-External-Analysis; a `β`-column table fragment. Needs the multi-session Camelot tuning track (see TODO.md "Cluster D-full Camelot tuning").
- **R4 column-aware reading-order** — `## Discussion` opens with a displaced Table-9 footnote + External-Analysis fragment; Method/Results subsections (`Data Analysis Strategy`, `Prevalence Estimates Associations…`) in wrong order. Needs the R4 column-aware re-extraction track.

These keep the per-commit canary hook tripping (single-audit samples a different subset each run) → routine commits need `SKIP_CANARY=1` until the surface is fixed or fully baselined. This is known debt, not a broken gate.

---

## Next-session opener

1. Read CLAUDE.md + memory `feedback_long_runs_die_on_this_machine` + this handoff.
2. **Decide the deferred track** with the user: Camelot-tables (B4) OR R4 reading-order. Each is its own multi-session effort.
3. Before any commit touching `docpluck/*.py`: the pre-commit canary hook now uses `--gate-new-only` (deferred baseline won't block; only NEW/regressed findings do). To baseline the deferred surface more completely, run `bash ~/.claude/skills/_shared/iterate-loop/canary-audit.sh --full docpluck-iterate` (double-audit union, all 5 canary papers) — this also satisfies I2 coverage that cycle 5 only did for the litmus.
4. **Push** `76a0248` to `main` when ready (push-to-main uses `--gate-new-only`). **Do NOT tag** until the canary set clears (tag push is the strict gate).
5. Optional substrate: expand `rotating_pool` past `rotation_size` (onboard efendic_2022_affect / maier_2023_collabra / xiao_2021_crsp per `canary.json::onboarding_status`).

---

## Verification evidence (cycle 5)

- `python -m pytest -q -n10` → **1870 passed, 27 skipped, 1 xfailed** (13:17).
- `python scripts/verify_corpus.py` → **PASS 26/26**.
- `bash ~/.claude/skills/_shared/iterate-loop/tests/test_canary_audit.sh` → **17/17**.
- Canary audit (`--quick`) on ip_feldman → **zero METADATA-LEAK / zero HALLUCINATION** findings (only the deferred TABLE/SECTION-BOUNDARY class).
- `iterate-gate --cycle 1` → FAIL (I2: litmus-only coverage; I3: ip_feldman still FAIL on the 10 deferred findings) — honest per rule 0e-bis; cycle NOT marked PASS, run NOT closed.
